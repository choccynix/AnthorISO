# AnthorOS Catalyst portage configuration

COMMON_FLAGS="-O2 -pipe"
CFLAGS="${COMMON_FLAGS}"
CXXFLAGS="${COMMON_FLAGS}"
FCFLAGS="${COMMON_FLAGS}"
FFLAGS="${COMMON_FLAGS}"

MAKEOPTS="-j2 -l2"
EMERGE_DEFAULT_OPTS="--jobs=2 --load-average=2"

# Build binary packages for everything emerged, and prefer binaries when available
# This means stage1 packages get reused as binaries in stage2
FEATURES="buildpkg getbinpkg binpkg-multi-instance"

# musl+llvm toolchain
CC="clang"
CXX="clang++"
AR="llvm-ar"
NM="llvm-nm"
RANLIB="llvm-ranlib"

USE="musl -systemd -pam -nls unicode"
ACCEPT_LICENSE="*"
ACCEPT_KEYWORDS="amd64"

GENTOO_MIRRORS="https://distfiles.gentoo.org"
