# AnthorOS Catalyst configuration

# Store builds and packages under /var/tmp/catalyst
storedir="/var/tmp/catalyst"

# Enable binary package caching between stages — packages built in stage1
# get reused in stage2 instead of recompiling
options="autoresume bindist kerncache pkgcache seedcache"

# Use all available cores
jobs=2
load-average=2
